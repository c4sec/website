// Deprecated: the service detail page is now rendered by <ServiceView>, which
// resolves both global-catalog and region-local services. Kept as a thin
// re-export for backwards compatibility.
export { ServiceView as ServiceDetail } from "./ServiceView";
